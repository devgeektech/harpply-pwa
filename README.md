# harpply-pwa

Projetct Setup:
1. install pnpm if not installed: npm install -g pnpm
2. pnpm --version
3. Run this to install dependencies: pnpm install
4. run project: pnpm dev

Node version: >=24



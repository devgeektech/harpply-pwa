model User {
  id        String   @id @default(uuid())
  email     String   @unique
  password  String
  role      Role     @default(INDIVIDUAL)
  createdAt DateTime @default(now())

  @@schema("auth")
}

enum Role {
  ADMIN
  EMPLOYER
  INDIVIDUAL
}